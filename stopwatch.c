#include <stdio.h>
int main() {
    int n,t;
    scanf("%d",&n);
    int simpan[n];
    int tambah=0;
    int jumlah=0;
    for(int i=0;i<n;i++) {
        scanf("%d",&t);
        simpan[tambah]=t;
        tambah++;    
    }
    if(n%2==0) {
            
    }
    else {
        printf("still running");
    }
}